version: "3"

services:
  docmost:
    image: docmost/docmost:0.23
    depends_on:
      - db
      - redis
    environment:
      APP_URL: "http://localhost:3000"
      APP_SECRET: "fbcdaa77435797ecae6587de210a917c33494a7831dcb5b24b1d8cdba99ee10d"
      DATABASE_URL: "postgresql://docmost:zacisa12@db:5432/docmost?schema=public"
      REDIS_URL: "redis://redis:6379"
      MAIL_DRIVER: smtp
      SMTP_HOST: smtp.gmail.com
      SMTP_PORT: 587
      SMTP_USERNAME: itrussell15@gmail.com
      SMTP_PASSWORD: xedokgizwkilblwmd
      SMTP_SECURE: false # Port 587 uses STARTTLS
      MAIL_FROM_ADDRESS: itrussell15@gmail.com
      MAIL_FROM_NAME: Docmost
      
    ports:
      - "3000:3000"
    restart: no
    volumes:
      - docmost:/app/data/storage

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: docmost
      POSTGRES_USER: docmost
      POSTGRES_PASSWORD: zacisa12
    restart: unless-stopped
    volumes:
      - db_data:/var/lib/postgresql/data

  redis:
    image: redis:7.2-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  docmost:
  db_data:
  redis_data:

